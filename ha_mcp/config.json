{
  "name": "Home Assistant MCP Server",
  "slug": "ha_mcp",
  "version": "1.0.0",
  "description": "MCP-style API for Home Assistant",
  "arch": ["aarch64"],
  "startup": "services",
  "boot": "auto",
  "host_network": true,
  "options": {
    "ha_token": ""
  },
  "schema": {
    "ha_token": "str"
  }
}
