{
  "name": "Home Assistant MCP Server",
  "slug": "ha_mcp",
  "version": "1.0.0",
  "description": "MCP-style API for Home Assistant",
  "startup": "services",
  "boot": "auto",
  "host_network": true,
  "arch": ["aarch64", "amd64"],
  "options": {
    "ha_token": ""
  },
  "schema": {
    "ha_token": "str"
  }
}
